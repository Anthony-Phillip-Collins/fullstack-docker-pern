{
  "name": "mongo",
  "version": "1.0.0",
  "description": "utility npm scripts for mongo with docker",
  "scripts": {
    "docker:dev:build": "docker compose -f docker-compose.dev.yml up --build",
    "docker:dev:up": "docker compose -f docker-compose.dev.yml up",
    "docker:dev:down": "docker compose -f docker-compose.dev.yml down --remove-orphans",
    "docker:dev:mongosh": "docker exec -it dev.mongo mongosh admin -u root -p 'example'"
  },
  "author": "Anthony Collins"
}
